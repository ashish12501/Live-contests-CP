#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int a, b,i=0;
        cin >> a >> b;
        if((b-a)%3==2) cout<<"NO"<<endl;
        else cout<<"YES"<<endl;
    }
}